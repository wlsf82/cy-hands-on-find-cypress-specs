describe('Users', { tags: ['@users', '@CRUD', '@happy-path'] }, () => {
  it('creates a user', () => {
    // some code that tests user creation
  })

  it('lists users', () => {
    // some code that tests listing users
  })

  it('updates user permissions', () => {
    // some code that tests updating a user's permissions
  })

  it('permanentently deletes a user', () => {
    // some code that tests deleting a user
  })
})
